
    <% incomeObjArray.forEach(element => { %>
        <tr>
            <th><%= element.id %></th>
            <th><%= element.currency %></th>
            <th><%= element.amount %></th>
            <th><%= element.source %></th>
            <th><%= element.date %></th>
        </tr>
   <% }); %>
